import modelExtend from 'dva-model-extend';
import { getInitState } from './const';
import effectRequest from './effect_request';
const model = <%= moduleName %>;

export default modelExtend(effectRequest, {
    namespace: model,
    state: getInitState(),
    effects: {
       
    },
    reducers: {
        
    },
    subscriptions: {
      
    }
});
